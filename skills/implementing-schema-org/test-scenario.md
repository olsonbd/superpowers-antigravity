# Pressure Scenario: Schema.org Compliance

**Context:** You are building a Payload CMS collection for `Venues`.
**Requirement:** The data model MUST be fully compliant with `Schema.org/Place`.

**Task:**

1. Define the Payload Collection Config for `Venues`.
2. Ensure it maps correctly to `Schema.org/Place`.
3. Include fields for address, geo-coordinates, and public access.

**Pressure:**

- Do this quickly.
- Use standard Payload field names.
- Don't over-engineer it.
